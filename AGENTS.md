# AGENTS.md

This repo uses **`CLAUDE.md`** as the single canonical agent guide.

👉 **Read [`./CLAUDE.md`](./CLAUDE.md) first.** It contains the routing table
that tells you which doc to open for your task, so you only load the context you
actually need.

Do not duplicate guidance here — update `CLAUDE.md` instead.
